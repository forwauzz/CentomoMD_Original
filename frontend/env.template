# Frontend Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# Site URL for consistent redirects (REQUIRED for auth flows)
VITE_SITE_URL=http://localhost:5173

# Supabase Configuration (REQUIRED for authentication)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Feature Flags
VITE_FEATURE_OUTPUT_LANGUAGE_SELECTION=true
VITE_ENABLE_SCHEMA_DRIVEN_FORMS=true

# Note: Replace the placeholder values above with your actual Supabase credentials
# You can find these in your Supabase project dashboard under Settings > API
#
# IMPORTANT: In Supabase Dashboard → Authentication:
# • Site URL: set to your real origin in prod; localhost in dev
# • Authorized Redirect URLs:
#   - http://localhost:5173/auth/callback
#   - https://<prod-domain>/auth/callback
# • Enable Google provider; set Client ID/Secret
