/**
 * Complete Case Filling Test
 * Creates a case, fills all major sections, and displays the complete JSON structure
 */

import 'dotenv/config';
import postgres from 'postgres';
import fs from 'fs';
import path from 'path';
import { fileURLToPath } from 'url';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

const DATABASE_URL = process.env.DATABASE_URL;

if (!DATABASE_URL) {
  console.error('❌ DATABASE_URL environment variable is required');
  console.error('   Make sure you have a .env file in the backend directory with DATABASE_URL set');
  console.error('   Or set it as an environment variable: DATABASE_URL=your_url node test-fill-complete-case.js');
  process.exit(1);
}

const sql = postgres(DATABASE_URL);

async function fillCompleteCase() {
  console.log('\n🧪 COMPLETE CASE FILLING TEST\n');
  console.log('='.repeat(70));

  try {
    // Get a test user_id (using first available user)
    const users = await sql`SELECT user_id FROM profiles LIMIT 1`;
    if (users.length === 0) {
      throw new Error('No users found in database');
    }
    const userId = users[0].user_id;
    console.log(`✅ Using user_id: ${userId}`);

    // Get a test clinic_id
    const clinics = await sql`SELECT id FROM clinics LIMIT 1`;
    const clinicId = clinics.length > 0 ? clinics[0].id : null;
    console.log(`✅ Using clinic_id: ${clinicId || 'null'}\n`);

    // Step 1: Create a new case
    console.log('1️⃣ Creating new case...');
    const caseResult = await sql`
      INSERT INTO cases (
        user_id,
        clinic_id,
        name,
        status,
        draft
      ) VALUES (
        ${userId},
        ${clinicId},
        'Test Case - Complete Fill',
        'draft',
        ${JSON.stringify({
          patientInfo: {},
          sections: {},
          metadata: {
            language: 'fr',
            status: 'draft',
            totalSections: 0,
            completedSections: 0,
            autoGeneratedName: false,
            lastAccessedAt: new Date().toISOString(),
            createdAt: new Date().toISOString(),
            updatedAt: new Date().toISOString()
          },
          ui: {
            activeSectionId: 'section_a',
            order: [],
            autosave: {}
          },
          sessions: []
        })}
      )
      RETURNING id, name, status, draft
    `;
    
    const caseId = caseResult[0].id;
    console.log(`✅ Case created: ${caseId}\n`);

    // Fetch current case to get draft structure
    let caseRecord = await sql`
      SELECT draft::jsonb as draft FROM cases WHERE id = ${caseId}
    `;
    // Ensure draft is parsed as JSON object
    let draft = caseRecord[0].draft;
    if (typeof draft === 'string') {
      draft = JSON.parse(draft);
    }

    // Helper function to update draft with section data
    const updateSection = (sectionId, sectionData, status = 'completed') => {
      if (!draft.sections) {
        draft.sections = {};
      }
      draft.sections[sectionId] = {
        data: sectionData,
        status: status,
        lastModified: new Date().toISOString()
      };
      draft.metadata.updatedAt = new Date().toISOString();
    };

    // Step 2: Fill Section A (Patient Information)
    console.log('2️⃣ Filling Section A (Patient Information)...');
    const sectionAData = {
      name: 'Jean Dupont',
      id: 'PAT-2025-001',
      dob: '1985-05-15',
      gender: 'M',
      phone: '514-555-1234',
      email: 'jean.dupont@example.com',
      address: '123 Rue Principale, Montréal, QC H1A 1A1'
    };
    updateSection('section_a', sectionAData);
    
    await sql`
      UPDATE cases
      SET draft = ${JSON.stringify(draft)}::jsonb,
          updated_at = NOW()
      WHERE id = ${caseId}
    `;
    console.log('✅ Section A filled\n');

    // Step 3: Fill Section B (Physician Information)
    console.log('3️⃣ Filling Section B (Physician Information)...');
    const sectionBData = {
      lastName: 'CENTOMO',
      firstName: 'Hugo',
      license: '1-18154',
      address: '5777 Boul. Gouin Ouest, Suite 370, Montréal, Qc, H4J 1E3',
      phone: '514-331-1400',
      email: 'adjointe.orthopedie@gmail.com'
    };
    updateSection('section_b', sectionBData);
    
    await sql`
      UPDATE cases
      SET draft = ${JSON.stringify(draft)}::jsonb,
          updated_at = NOW()
      WHERE id = ${caseId}
    `;
    console.log('✅ Section B filled\n');

    // Step 4: Fill Section C (Mandat)
    console.log('4️⃣ Filling Section C (Mandat)...');
    const sectionCData = {
      mandatText: 'Évaluation médicale indépendante demandée par la CNESST dans le cadre de l\'application de l\'article 204 de la LATMP. Le mandat consiste à évaluer la lésion professionnelle signalée et à produire un rapport détaillé.'
    };
    updateSection('section_c_mandat', sectionCData);
    
    await sql`
      UPDATE cases
      SET draft = ${JSON.stringify(draft)}::jsonb,
          updated_at = NOW()
      WHERE id = ${caseId}
    `;
    console.log('✅ Section C (Mandat) filled\n');

    // Step 5: Fill Section C Modalité
    console.log('5️⃣ Filling Section C (Modalité)...');
    const sectionCModaliteData = {
      modaliteText: `L'évaluation suivante s'est tenue dans les locaux de la Clinique Médicale de l'Or et des Bois, Val-d'Or. Nous avons clairement expliqué à notre mandat d'évaluateur indépendant désigné par la CNESST dans le cadre de l'application de l'article 204 de la LATMP. Nous lui avons précisé que nous n'agirons pas en tant que médecins traitants. Notre rapport d'évaluation sera d'abord envoyé à la CNESST.

Nous avons procédé au questionnaire subjectif ainsi qu'à un examen physique détaillé en relation avec les lésions à évaluer, nous nous sommes assurés à la fin de l'entrevue d'avoir couvert l'ensemble de la problématique.

Nous avons revu le dossier CNESST de même que le dossier médical. Nous avons pu consulter l'ensemble des rapports et des bilans radiologiques réalisés dans le cadre de l'évaluation de la lésion.

L'entrevue s'est effectuée cordialement, le travailleur participait pleinement à son entrevue. L'entrevue s'est déroulée entre [heure].

À la fin de l'entrevue, nous avons demandé à s'il avait d'autres commentaires ou informations à nous divulguer. Ce dernier nous a répondu par la négative.`,
      duree: '60 minutes',
      modaliteCommentaires: 'Examen complet effectué sans complications.'
    };
    updateSection('section_c_modalite', sectionCModaliteData);
    
    await sql`
      UPDATE cases
      SET draft = ${JSON.stringify(draft)}::jsonb,
          updated_at = NOW()
      WHERE id = ${caseId}
    `;
    console.log('✅ Section C (Modalité) filled\n');

    // Step 6: Fill Section 7 (History)
    console.log('6️⃣ Filling Section 7 (History)...');
    const section7Data = {
      mainContent: `HISTOIRE DE LA LÉSION

Le patient, M. Jean Dupont, âgé de 39 ans, rapporte une lésion survenue le [date] alors qu'il soulevait une charge lourde lors de son travail. Il décrit une douleur soudaine et intense au niveau lombaire droit, irradiant vers la fesse droite et la cuisse postérieure.

L'évolution depuis l'accident montre une amélioration progressive avec des périodes de récidive lors d'efforts physiques. Le patient bénéficie actuellement de traitements de physiothérapie et de médication anti-inflammatoire.

ANTÉCÉDENTS MÉDICAUX

Antécédents médicaux non contributifs à la lésion actuelle. Pas d'antécédents de chirurgie lombaire.`
    };
    updateSection('section_7', section7Data);
    
    await sql`
      UPDATE cases
      SET draft = ${JSON.stringify(draft)}::jsonb,
          updated_at = NOW()
      WHERE id = ${caseId}
    `;
    console.log('✅ Section 7 filled\n');

    // Step 7: Fill Section 8 (Physical Examination)
    console.log('7️⃣ Filling Section 8 (Physical Examination)...');
    const section8Data = {
      physicalExam: `EXAMEN PHYSIQUE

Examen physique effectué le [date]. Le patient se présente en position debout, marche avec une légère boiterie du côté droit.

Examen de la colonne lombaire:
- Inspection: Pas de déformation visible
- Palpation: Sensibilité à la palpation des articulations sacro-iliaques droites
- Mobilité: Flexion limitée à 60°, extension limitée à 15°
- Tests neurologiques: Force musculaire normale, réflexes présents et symétriques

Examen neurologique complet effectué sans déficit neurologique significatif.`
    };
    updateSection('section_8', section8Data);
    
    await sql`
      UPDATE cases
      SET draft = ${JSON.stringify(draft)}::jsonb,
          updated_at = NOW()
      WHERE id = ${caseId}
    `;
    console.log('✅ Section 8 filled\n');

    // Step 8: Update metadata
    console.log('8️⃣ Updating case metadata...');
    draft.metadata.completedSections = Object.keys(draft.sections).length;
    draft.metadata.totalSections = 15;
    
    await sql`
      UPDATE cases
      SET draft = ${JSON.stringify(draft)}::jsonb,
          status = 'in_progress',
          updated_at = NOW()
      WHERE id = ${caseId}
    `;
    console.log('✅ Metadata updated\n');

    // Step 9: Retrieve complete case
    console.log('9️⃣ Retrieving complete case data...\n');
    const completeCase = await sql`
      SELECT id, name, status, draft::jsonb as draft, created_at, updated_at
      FROM cases
      WHERE id = ${caseId}
    `;
    
    const caseData = completeCase[0];
    
    // Parse draft if it's a string
    let parsedDraft = caseData.draft;
    if (typeof parsedDraft === 'string') {
      parsedDraft = JSON.parse(parsedDraft);
    }
    
    console.log('='.repeat(70));
    console.log('📄 COMPLETE CASE JSON STRUCTURE');
    console.log('='.repeat(70));
    console.log('\n');

    // Display formatted JSON
    console.log(JSON.stringify(parsedDraft, null, 2));
    
    console.log('\n' + '='.repeat(70));
    console.log('📊 CASE SUMMARY');
    console.log('='.repeat(70));
    console.log(`Case ID: ${caseData.id}`);
    console.log(`Case Name: ${caseData.name}`);
    console.log(`Status: ${caseData.status}`);
    console.log(`Created: ${caseData.created_at}`);
    console.log(`Updated: ${caseData.updated_at}`);
    console.log(`\nSections Filled: ${Object.keys(parsedDraft.sections || {}).length}`);
    console.log(`Sections: ${Object.keys(parsedDraft.sections || {}).join(', ')}`);
    console.log('\n' + '='.repeat(70));
    console.log('✅ TEST COMPLETED SUCCESSFULLY!\n');

    // Save to file
    const outputsDir = path.join(__dirname, '..', 'outputs');
    if (!fs.existsSync(outputsDir)) {
      fs.mkdirSync(outputsDir, { recursive: true });
    }
    const outputPath = path.join(outputsDir, `test-case-complete-${caseId}.json`);
    fs.writeFileSync(outputPath, JSON.stringify(parsedDraft, null, 2), 'utf-8');
    console.log(`📁 Complete JSON saved to: ${outputPath}\n`);

  } catch (error) {
    console.error('\n❌ TEST FAILED:', error.message);
    console.error('Error details:', error);
    process.exit(1);
  } finally {
    await sql.end();
  }
}

fillCompleteCase();

